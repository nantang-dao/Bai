# 改动记录：链上转账记录查询 & 活动付款精确追踪

> 本文件记录 MycoSeed 项目中链上转账记录查询功能的完整改动历史，包含两次提交。

---

## 提交总览

| 提交 | 分支 | 日期 | 说明 |
|------|------|------|------|
| `b8ffdfe` | `master` | 2026-05-23 | feat: 添加链上转账记录查询功能（模糊匹配） |
| `224a88b` | `master` | 2026-05-23 | feat: 活动付款精确追踪（精确匹配升级） |

---

# 第一次提交：`b8ffdfe` — 链上转账记录查询（模糊匹配）

> Commit: `b8ffdfe` | 日期: 2026-05-23

## 背景

MycoSeed 任务奖励通过 Semi 钱包在 Optimism 链上转账（NT 积分）。之前只有人工标记 `transferred_at`，无法查看真实链上转账状态。本次改动从 semi 项目迁移 Alchemy 查询功能，让发包方和接包方都能看到真实转账情况。

## 改动总览

| 类型 | 文件数 | 说明 |
|------|--------|------|
| 后端新建 | 3 | alchemy.ts, transactionsController.ts, migration 030 |
| 后端修改 | 2 | tasksController.ts, index.ts |
| 前端修改 | 3 | api.ts, useApi.ts, tasks/[id].vue, member/[id].vue |
| 合约项目 | — | contracts-project/ 整个目录（含 RemarkLogicV1.sol 等） |
| 基础设施 | — | .gitignore, .env.example, Dockerfile, fly.toml 等 |

注：本次提交包含整个项目的初始提交（196 个文件），以下仅列出功能相关改动。

---

### 1. `mycoseed-backend/db/migrations/030_create_transactions_table.sql`（新建）

**完整内容**：
```sql
CREATE TABLE IF NOT EXISTS transactions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tx_hash VARCHAR(128) NOT NULL,
    chain VARCHAR(20) NOT NULL DEFAULT 'optimism',
    sender_address VARCHAR(128),
    receiver_address VARCHAR(128),
    amount NUMERIC(30, 0),           -- wei 值
    currency VARCHAR(10) DEFAULT 'NT',
    status VARCHAR(20) DEFAULT 'success',
    memo TEXT,
    task_id UUID REFERENCES tasks(id) ON DELETE SET NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_transactions_tx_hash ON transactions(tx_hash);
CREATE INDEX IF NOT EXISTS idx_transactions_task_id ON transactions(task_id);
CREATE INDEX IF NOT EXISTS idx_transactions_sender ON transactions(sender_address);
CREATE INDEX IF NOT EXISTS idx_transactions_receiver ON transactions(receiver_address);
```

新增 `transactions` 表，存储从 Alchemy 查询到的 ERC20 Transfer 事件，关联到具体任务。

---

### 2. `mycoseed-backend/src/services/alchemy.ts`（新建）

**初始版本**，使用 `alchemy-sdk` 的 `getAssetTransfers` API 进行模糊匹配。

**核心函数**：`getNtTransfersBetween(senderAddress, receiverAddress)`
- 调用 `alchemy.core.getAssetTransfers` 按发送方+接收方地址查询 ERC20 Transfer
- 无法区分同一对地址之间的多笔转账（同金额时会混淆）
- 返回 `TransferResult[]`（txHash, from, to, value, blockNum, timestamp）

**常量**：
- `NT_TOKEN = '0x7563cb33148cD2b929ed85e69F697be13b515Bd0'` — NT 积分合约地址

---

### 3. `mycoseed-backend/src/controllers/transactionsController.ts`（新建）

**初始版本**，实现 `GET /api/transactions?task_id=xxx` 端点。

**查询流程**：
1. 先查本地 `transactions` 表缓存（`task_id = taskId`）
2. 有缓存 → 直接返回
3. 无缓存 → 查 `tasks` 表获取 `creator_id` 和 `claimer_id`
4. 查 `users` 表获取双方钱包地址
5. 调 `getNtTransfersBetween(creator_wallet, claimer_wallet)` 模糊匹配
6. 匹配到 → 存入 `transactions` 表 → 返回结果

---

### 4. `mycoseed-backend/src/controllers/tasksController.ts`（修改）

**改动**：修复 `getAllTasks` 函数遗漏 `transferred_at` 字段的问题。

将所有 `supabase.from('tasks').select(...)` 调用中的字段列表补充了 `transferred_at`，确保前端能获取到任务的转账标记时间。

涉及位置（共 6 处 select 语句）：
- `getTask()` — 第 416 行
- `getTaskInfo()` — 第 524 行
- `getAllTasks()` 批量查询 — 第 635、643 行
- `getTaskDetail()` — 第 875 行
- `createTask()` — 第 1203 行

---

### 5. `mycoseed-backend/src/index.ts`（修改）

**改动**：注册新的 `GET /api/transactions` 路由。

```typescript
import { getTransactions } from './controllers/transactionsController'
// ...
app.get('/api/transactions', authenticate, getTransactions)
```

---

### 6. `mycoseed-frontend/utils/api.ts`（修改）

**新增函数**：
```typescript
// 查询任务的链上转账记录
export const getTaskTransactions = async (taskId: string, baseUrl: string) => {
  const response = await fetch(`${baseUrl}/api/transactions?task_id=${taskId}`)
  if (!response.ok) throw new Error('Failed to fetch transactions')
  return response.json()
}
```

---

### 7. `mycoseed-frontend/composables/useApi.ts`（修改）

**新增**：
```typescript
getTaskTransactions: (taskId: string) => api.getTaskTransactions(taskId, apiBaseUrl),
```

---

### 8. `mycoseed-frontend/pages/tasks/[id].vue`（修改）

**改动**：发包方和接包方都能看到转账状态。

- 新增 `taskTransactions` 响应式变量
- `load()` 函数中加载链上转账记录
- 发包方视图：显示"已转账"标签（含 Etherscan 链接）
- 接包方视图：显示"已收款"标签（含 Etherscan 链接）
- 修复转账金额显示 bug（从 wei 转换为 NT）

---

### 9. `mycoseed-frontend/pages/member/[id].vue`（修改）

**改动**：成员页新增转账状态显示。

- 新增两个标签：已转账 / 未转账
- 新增五个筛选条件
- 每个任务卡片显示转账状态标签（已转账/未转账）
- 已转账任务显示 Etherscan 交易链接

---

### 10. 合约项目 `contracts-project/`（新建）

完整目录，包含：
- `contracts/RemarkLogicV1.sol` — Remark 合约（saveRemark / getRemarks）
- `contracts/Proxy.sol` — 可升级代理合约
- `contracts/TaskPoolLogicV1.sol` — 任务池合约
- `scripts/remark/deploy.ts` — Remark 合约部署脚本
- `scripts/remark/upgrade.ts` — Remark 合约升级脚本
- `hardhat.config.ts` — Hardhat 配置（Optimism 网络）

---

# 第二次提交：`224a88b` — 活动付款精确追踪（精确匹配升级）

> Commit: `224a88b` | 日期: 2026-05-23

## 背景

第一次提交使用的模糊匹配（`getNtTransfersBetween`）无法区分同一对地址的多笔转账。Semi 钱包在转账时已自动调用 `saveRemark(keccak256(poolId), keccak256(taskId), ...)` 将活动/任务 ID 写入链上 RemarkSaved 事件。本次改动将查询逻辑从模糊匹配升级为精确匹配，并新增活动付款追踪功能。

## 改动总览

| 类型 | 文件数 | 说明 |
|------|--------|------|
| 后端修改 | 4 | alchemy.ts, transactionsController.ts, remarkContract.ts, .env |
| 后端新建 | 1 | migration 031 |
| 前端修改 | 3 | api.ts, useApi.ts, events/[eventId].vue |
| 依赖变更 | 1 | 新增 ethers ^6.16.0 |

---

### 1. `mycoseed-backend/src/services/alchemy.ts`（重写）

**改动类型**：重写（删除旧函数，新增 3 个函数）

**删除的内容**：
- 删除了 `getNtTransfersBetween(senderAddress, receiverAddress)` 模糊匹配函数
- 该函数通过 `alchemy.core.getAssetTransfers` 按发送方+接收方查 ERC20 Transfer，无法区分同一对地址的多笔转账
- 删除了 `AssetTransfersCategory`, `SortingOrder` 的 import（不再需要）

**新增的 import**：
```typescript
import { keccak256, toUtf8Bytes } from 'ethers'
import { remarkSavedEventAbi } from '../utils/remarkContract'
```

**新增的常量**：
```typescript
const REMARK_ADDRESS = process.env.REMARK_PROXY_ADDRESS || '0xe7c8244d80f4a3c2e4c8fe04197d38e87571df58'
const REMARK_SAVED_TOPIC0 = keccak256(toUtf8Bytes('RemarkSaved(uint256,uint256,string,string,uint256)'))
```

**新增的工具函数**：
```typescript
function hashUuid(uuid: string): string {
  return keccak256(toUtf8Bytes(uuid))
}
```
将 UUID 字符串转为 `uint256` 哈希，与 Semi 钱包在链上写入时使用的算法一致。

**新增的接口**：
```typescript
interface RemarkEvent {
  txHash: string
  poolId: string
  taskId: string
  senderRemark: string
  receiverRemark: string
  timestamp: string
  blockNumber: number
}

interface TransferDetail {
  from: string
  to: string
  value: string
  blockTimestamp: string
}
```

**新增函数 1**：`getRemarkByTaskId(taskId: string): Promise<RemarkEvent[]>`
- 功能：按 `keccak256(taskId)` 精确查询链上 `RemarkSaved` 事件
- 逻辑：
  1. 计算 `taskIdHash = keccak256(toUtf8Bytes(taskId))`
  2. 调用 `alchemy.core.getLogs` 查询合约 `REMARK_ADDRESS` 的事件
  3. `topics[0]` = `REMARK_SAVED_TOPIC0`，`topics[2]` = `taskIdHash`（taskId 是第 2 个 indexed 参数）
  4. 用 `ethers.Interface` 解析事件数据
- 用途：任务付款精确匹配

**新增函数 2**：`getRemarkByPoolId(poolId: string): Promise<RemarkEvent[]>`
- 功能：按 `keccak256(poolId)` 精确查询链上 `RemarkSaved` 事件
- 逻辑：与 `getRemarkByTaskId` 类似，但过滤 `topics[1]` = `poolIdHash`（poolId 是第 1 个 indexed 参数）
- 用途：活动付款精确匹配

**新增函数 3**：`getTransferByTxHash(txHash: string): Promise<TransferDetail | null>`
- 功能：用交易哈希查询同一笔交易中的 ERC20 Transfer 详情
- 逻辑：
  1. 调用 `alchemy.core.getTransactionReceipt(txHash)` 获取交易回执
  2. 从回执的 `logs` 中找到 NT Token 合约（`0x7563cb...`）的 Transfer 事件
  3. 解析 `topics[1]`（from）、`topics[2]`（to）、`data`（value）
  4. 用 `alchemy.core.getBlock` 获取区块时间戳
- 用途：获取实际转账金额和时间

---

### 2. `mycoseed-backend/src/controllers/transactionsController.ts`（重写）

**改动类型**：重写（整体逻辑从模糊匹配改为精确匹配）

**import 变更**：
```diff
-import { getNtTransfersBetween } from '../services/alchemy'
+import { getRemarkByTaskId, getRemarkByPoolId, getTransferByTxHash } from '../services/alchemy'
```

**主函数 `getTransactions` 改动**：
- 旧：只接受 `task_id` 参数，查不到则调 `getNtTransfersBetween` 模糊匹配
- 新：接受 `task_id` 或 `event_id` 参数，分发到对应处理函数

**删除的逻辑**（旧的 task_id 查询）：
- 删除了查 `tasks` 表获取 `creator_id`/`claimer_id` 的步骤
- 删除了查 `users` 表获取双方钱包地址的步骤
- 删除了调 `getNtTransfersBetween(creator_wallet, claimer_wallet)` 的模糊匹配

**新增函数 1**：`handleTaskTransaction(req, res, taskId)`
- 查询流程：
  1. 先查本地 `transactions` 表缓存（`task_id = taskId`），有则直接返回
  2. 无缓存 → 调 `getRemarkByTaskId(taskId)` 查链上 RemarkSaved 事件
  3. 匹配到 → 拿到 `txHash` → 调 `getTransferByTxHash(txHash)` 获取 Transfer 详情
  4. 组装记录（含 `actual_amount`, `expected_amount: null`）存入 `transactions` 表
  5. 返回结果

**新增函数 2**：`handleEventTransactions(req, res, eventId)`
- 查询流程：
  1. 先查本地 `transactions` 表缓存（`event_id = eventId`），有则直接返回
  2. 无缓存 → 调 `getRemarkByPoolId(eventId)` 查链上 RemarkSaved 事件
  3. 每条 remark 用 `txHash` 查 ERC20 Transfer 详情
  4. 查 `community_event_options` 表获取活动标价（`expected_amount`）
  5. 组装记录（含 `actual_amount`, `expected_amount`, `event_id`）存入 `transactions` 表
  6. 返回结果

**新增的记录字段**：
- `actual_amount`：用户实际转账金额（wei），从链上 Transfer 事件读取
- `expected_amount`：活动标价（wei），从 `community_event_options.price * 1e18` 计算
- `event_id`：关联的活动 ID

---

### 3. `mycoseed-backend/src/utils/remarkContract.ts`（追加）

**改动类型**：追加（保留原有内容，新增事件 ABI）

**保留不变**：
```typescript
export const remarkProxyGetRemarksAbi = [
  {
    inputs: [{ name: 'taskId', type: 'string', internalType: 'string' }],
    name: 'getRemarks',
    outputs: [
      { name: 'senderRemark', type: 'string', internalType: 'string' },
      { name: 'receiverRemark', type: 'string', internalType: 'string' },
      { name: 'timestamp', type: 'uint256', internalType: 'uint256' },
    ],
    stateMutability: 'view',
    type: 'function',
  },
] as const
```

**新增**：
```typescript
export const remarkSavedEventAbi = [
  {
    anonymous: false,
    inputs: [
      { indexed: true, name: 'poolId', type: 'uint256' },
      { indexed: true, name: 'taskId', type: 'uint256' },
      { indexed: false, name: 'senderRemark', type: 'string' },
      { indexed: false, name: 'receiverRemark', type: 'string' },
      { indexed: false, name: 'timestamp', type: 'uint256' },
    ],
    name: 'RemarkSaved',
    type: 'event',
  },
] as const
```

说明：`poolId` 和 `taskId` 是 `indexed` 参数，会被编码为 `topics`，可用于链上日志过滤。`senderRemark`、`receiverRemark`、`timestamp` 是非 indexed 参数，存储在 `data` 字段中。

---

### 4. `mycoseed-backend/.env`（追加）

**新增**：
```
REMARK_PROXY_ADDRESS=0xe7c8244d80f4a3c2e4c8fe04197d38e87571df58
```

说明：Remark 合约在 Optimism 上的部署地址。`alchemy.ts` 中通过 `process.env.REMARK_PROXY_ADDRESS` 读取。

---

### 5. `mycoseed-backend/db/migrations/031_add_event_payment_support.sql`（新建）

**完整内容**：
```sql
-- 活动关联
ALTER TABLE transactions ADD COLUMN IF NOT EXISTS event_id UUID REFERENCES community_events(id) ON DELETE SET NULL;
CREATE INDEX IF NOT EXISTS idx_transactions_event_id ON transactions(event_id);

-- 实际付款金额（wei），用户可能改金额
ALTER TABLE transactions ADD COLUMN IF NOT EXISTS actual_amount NUMERIC(30, 0);

-- 标价（预期金额）
ALTER TABLE transactions ADD COLUMN IF NOT EXISTS expected_amount NUMERIC(30, 0);

COMMENT ON COLUMN transactions.event_id IS '关联的活动 ID';
COMMENT ON COLUMN transactions.actual_amount IS '实际付款金额（wei），用户可能修改转账金额';
COMMENT ON COLUMN transactions.expected_amount IS '标价/预期金额（wei）';
```

新增 3 列：
| 列名 | 类型 | 说明 |
|------|------|------|
| `event_id` | UUID | 关联 `community_events(id)`，ON DELETE SET NULL |
| `actual_amount` | NUMERIC(30,0) | 用户实际付款金额（wei） |
| `expected_amount` | NUMERIC(30,0) | 活动标价/预期金额（wei） |

---

### 6. `mycoseed-backend/package.json` + `package-lock.json` + `yarn.lock`

**新增依赖**：
```json
"ethers": "^6.16.0"
```

用途：提供 `keccak256`、`toUtf8Bytes`、`Interface` 函数，用于计算 UUID 哈希和解析链上事件数据。

---

### 7. `mycoseed-frontend/utils/api.ts`（追加）

**新增函数**（第 807 行之后）：
```typescript
// 查询活动的链上付款记录
export const getEventTransactions = async (eventId: string, baseUrl: string) => {
  const response = await fetch(`${baseUrl}/api/transactions?event_id=${eventId}`)
  if (!response.ok) throw new Error('Failed to fetch event transactions')
  return response.json()
}
```

---

### 8. `mycoseed-frontend/composables/useApi.ts`（追加）

**新增**（第 128 行之后）：
```typescript
getEventTransactions: (eventId: string) => api.getEventTransactions(eventId, apiBaseUrl),
getWalletAddressByUserId: (userId: string) => api.getWalletAddressByUserId(userId, apiBaseUrl),
```

---

### 9. `mycoseed-frontend/pages/community/[id]/events/[eventId].vue`（追加）

#### 改动 1：新增响应式变量（第 272 行之后）

```typescript
const eventTransactions = ref<any[]>([])
const userWalletCache = ref<Record<string, string>>({}) // userId -> wallet address
```

#### 改动 2：新增 `paymentMap` 计算属性（第 325 行之后）

```typescript
const paymentMap = computed(() => {
  const map: Record<string, { status: 'paid' | 'partial' | 'pending'; amount: string; txHash: string }> = {}
  if (!detail.value) return map

  const hasPaidOptions = detail.value.event.options?.some((o: any) => o.price > 0)
  if (!hasPaidOptions) return map

  // 构建 wallet -> userId 反向映射
  const walletToUser: Record<string, string> = {}
  for (const [uid, wallet] of Object.entries(userWalletCache.value)) {
    if (wallet) walletToUser[wallet.toLowerCase()] = uid
  }

  // 匹配交易到用户
  for (const tx of eventTransactions.value) {
    const sender = tx.sender_address?.toLowerCase()
    if (!sender) continue
    const userId = walletToUser[sender]
    if (!userId) continue

    const actualAmount = Number(tx.actual_amount || tx.amount || 0)
    const expectedAmount = Number(tx.expected_amount || 0)

    let status: 'paid' | 'partial' | 'pending' = 'paid'
    if (expectedAmount > 0 && actualAmount < expectedAmount) {
      status = 'partial'
    }

    map[userId] = { status, amount: String(actualAmount), txHash: tx.tx_hash }
  }
  return map
})
```

逻辑：
1. 如果活动没有付费选项（所有 option.price = 0），返回空 map
2. 构建 `钱包地址 → userId` 反向映射
3. 遍历链上交易记录，按 `sender_address` 匹配到用户
4. 比较 `actual_amount` 和 `expected_amount`：
   - `actual >= expected` → `paid`（已付款）
   - `actual < expected` → `partial`（付款不足）
   - 无记录 → `pending`（待付款）

#### 改动 3：`load()` 函数新增数据加载（第 388 行之后）

```typescript
// 加载活动付款记录
try {
  eventTransactions.value = await api.getEventTransactions(eventId.value)
} catch {
  eventTransactions.value = []
}
// 加载参与者钱包地址（用于匹配链上付款）
if (detail.value?.participations?.length) {
  const userIds = [...new Set(detail.value.participations.map((p: any) => p.userId))]
  const walletMap: Record<string, string> = {}
  await Promise.all(
    userIds.map(async (uid: string) => {
      try {
        const addr = await api.getWalletAddressByUserId(uid)
        if (addr) walletMap[uid] = addr
      } catch { /* ignore */ }
    })
  )
  userWalletCache.value = walletMap
}
```

#### 改动 4：`doRegister()` 构建 Semi URL 时传 `pool_uuid`（第 481 行）

```diff
-      const url = buildSemiTransferUrl(payTo, String(price), { semiAppUrl, memo })
+      const url = buildSemiTransferUrl(payTo, String(price), { semiAppUrl, memo, pool_uuid: detail.value.event.id })
```

说明：将 `event.id` 作为 `pool_uuid` 传给 Semi 钱包，Semi 转账时会自动调用 `saveRemark(keccak256(event_id), ...)` 写入链上，实现活动与转账的精确关联。

#### 改动 5：参与矩阵（pack 类型）每个 cell 显示付款状态（第 109 行之后）

```html
<template v-if="row.cells[o.id].price > 0 && row.user?.id">
  <span v-if="paymentMap[row.user.id]?.status === 'paid'" class="text-[10px] text-green-600">
    💰已付
  </span>
  <span v-else-if="paymentMap[row.user.id]?.status === 'partial'" class="text-[10px] text-orange-500">
    💰不足
  </span>
  <span v-else class="text-[10px] text-orange-500">
    💰待付
  </span>
</template>
```

#### 改动 6：报名列表每个参与者卡片显示付款状态（第 148 行之后）

```html
<template v-if="detail.event.options?.some((o: any) => o.price > 0)">
  <span v-if="paymentMap[p.userId]?.status === 'paid'" class="text-xs px-1.5 py-0.5 rounded bg-green-100 text-green-700">
    已付款
  </span>
  <span v-else-if="paymentMap[p.userId]?.status === 'partial'" class="text-xs px-1.5 py-0.5 rounded bg-orange-100 text-orange-700">
    付款不足
  </span>
  <span v-else class="text-xs px-1.5 py-0.5 rounded bg-orange-100 text-orange-700">
    待付款
  </span>
</template>
```

#### 改动 7：报名列表显示 Etherscan 交易链接（第 173 行之后）

```html
<p v-if="paymentMap[p.userId]?.txHash" class="text-xs text-primary mt-1">
  <a :href="`https://optimistic.etherscan.io/tx/${paymentMap[p.userId].txHash}`" target="_blank" rel="noopener" class="underline">
    查看交易
  </a>
</p>
```

---

## 未改动的文件（确认保持不变）

| 文件 | 说明 |
|------|------|
| `mycoseed-frontend/pages/tasks/[id].vue` | 任务详情页，显示逻辑不变 |
| `mycoseed-frontend/pages/member/[id].vue` | 成员页，显示逻辑不变 |
| `mycoseed-backend/src/controllers/tasksController.ts` | 任务控制器，不变 |
| `mycoseed-backend/src/index.ts` | 路由注册，不变（已有 `GET /api/transactions`） |
| `mycoseed-backend/db/migrations/030_create_transactions_table.sql` | 原始表结构，不变 |

---

## 部署清单

1. 在 Supabase 执行 `030_create_transactions_table.sql`（如未执行）
2. 在 Supabase 执行 `031_add_event_payment_support.sql`
3. 确认后端已安装 `ethers` 依赖（`npm install` 或 `yarn install`）
4. 确认后端 `.env` 包含 `REMARK_PROXY_ADDRESS=0xe7c8244d80f4a3c2e4c8fe04197d38e87571df58`
5. 重启后端服务
