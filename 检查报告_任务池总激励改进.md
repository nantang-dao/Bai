# BAI 任务池改进检查报告

## 📋 问题总结

你已经将任务发布系统从"人数 + 一人金额"改为"任务池总积分"，但**后端和前端存在多个逻辑不一致和缺失的约束**。

---

## 🔴 严重问题（必须修复）

### 1. **后端缺失：子任务总金额约束验证**

**影响范围**：任意Manager都可以创建金额超过任务池总激励的子任务而无警告

| 位置 | 函数 | 问题 |
|------|------|------|
| [taskInfoController.ts#L199](src/controllers/taskInfoController.ts#L199) | `createSubtask` | ❌ 无验证：创建时未检查金额 |
| [taskInfoController.ts#L260](src/controllers/taskInfoController.ts#L260) | `patchSubtask` | ❌ 无验证：编辑时未检查金额 |
| [taskInfoController.ts#L350](src/controllers/taskInfoController.ts#L350) | `finalizeSubtasks` | ❌ **关键**：定稿前无金额总和校验 |

**当前流程**：
```
┌─ 创建 task_info（plannedLockNt = 100）
│
├─ Manager 创建子任务1（rewardNt = 60）✓ 无验证
├─ Manager 创建子任务2（rewardNt = 60）✓ 无验证 [总计120 > 100 ⚠️]
│
└─ 点击定稿 → 直接标记 subtasks_finalized=true [⚠️ 问题成立]
```

**需要添加的验证**（参考 [checkSubtaskSubmitDeadlineGate](src/controllers/taskInfoController.ts#L155)）：
```typescript
// 计算所有子任务的总金额
const totalSubtaskReward = sum(
  subtasks.map(s => {
    const reward = s.rewardNt ?? 0
    const participants = s.participantLimit ?? 1
    return reward * participants
  })
)

if (totalSubtaskReward > poolTotalIncentive) {
  return Error('子任务总金额超过任务池总激励') // 400
}
```

---

### 2. **后端逻辑错误：patchTaskPoolDraft 计算 planned_lock_nt**

**位置**：[taskInfoController.ts#L829](src/controllers/taskInfoController.ts#L829)

**当前代码**：
```typescript
const plannedLockNt = rewardNum * participantLimit  // ⚠️ 错误！
```

**问题**：
- 编辑页面传来的 `reward` 是旧字段（"每人积分"），不是"任务池总积分"
- 再乘以 `participantLimit` 会重复计算
- 用户期望的是直接替换总金额

**修复方案**：
根据前端传来的是哪个字段来判断：
```typescript
// 前端应该在修改时明确说明是修改总金额还是修改"人数+每人金额"
// 或者在PATCH body中传递 plannedLockNt 而不是计算

// 选项1：如果编辑页改为传"总金额"
if (body.plannedLockNt != null) {
  plannedLockNt = body.plannedLockNt
} else {
  // 选项2：兼容旧方式（reward × participantLimit）
  plannedLockNt = rewardNum * participantLimit
}
```

---

### 3. **前端编辑页面与创建页面不一致**

**创建页面** ([create.vue#L40](pages/tasks/pool/create.vue#L40))：
```vue
<label>任务池总积分 *</label>
<input v-model="form.plannedLockNt" type="number" />
```
✅ 正确：接收单一的"总金额"

**编辑页面** ([edit.vue#L57-L65](pages/tasks/pool/[taskInfoId]/edit.vue#L57-L65))：
```vue
<label>参与人数</label>
<input v-model.number="form.participantLimit" />
<label>每人积分</label>
<input v-model="form.reward" />
```
❌ 错误：仍然用"人数 + 每人金额"的旧方式

**影响**：
- 创建为100（总金额），编辑时显示 reward=? participantLimit=? （需要反推）
- 用户无法看到并编辑"任务池总积分"

**修复方案**：更新编辑页面与创建页面保持一致

---

## 🟡 中等问题

### 4. **前端管理页缺失子任务金额总计展示**

**位置**：[manage.vue#L700+](pages/tasks/pool/[taskInfoId]/manage.vue#L700)

**当前**：显示每条子任务的信息，但无总计
```vue
<li v-for="s in subtasks">
  {{ s.title }}
  奖励: {{ s.rewardNt }} × {{ s.participantLimit }} = {{ s.rewardNt * s.participantLimit }}
  ...
</li>
```

**缺失**：
```vue
<!-- 需要添加 -->
<div class="mt-2 pt-2 border-t border-border">
  <div>任务池总积分：{{ taskMeta.plannedLockNt }}</div>
  <div>子任务已分配：{{ totalSubtaskRewardCalculated }}</div>
  <div :class="totalSubtaskRewardCalculated > taskMeta.plannedLockNt ? 'text-red-500' : 'text-green-500'">
    剩余：{{ taskMeta.plannedLockNt - totalSubtaskRewardCalculated }}
  </div>
</div>
```

**风险**：Manager 在创建多个子任务时可能不知道是否超额

---

## 🟢 数据字段确认

### task_info 表
| 字段 | 类型 | 说明 | 检查状态 |
|------|------|------|--------|
| `title` | TEXT | 任务名称 | ✅ 完整 |
| `planned_lock_nt` | NUMERIC(24,8) | **任务池总激励** | ✅ 存在 |
| `use_taskpool` | BOOLEAN | 是否强制走链上TaskPool | ✅ 存在 |
| `allow_split` | BOOLEAN | 是否允许拆分 | ✅ 存在 |
| `subtasks_finalized` | BOOLEAN | 子任务是否定稿 | ✅ 存在 |

### task_subtasks 表
| 字段 | 类型 | 说明 | 检查状态 |
|------|------|------|--------|
| `max_amount_nt` | NUMERIC(24,8) | 单子任务最大金额 | ⚠️ 存在但无约束验证 |
| `participant_limit` | INTEGER | 参与人数 | ✅ 存在 |
| `reward_nt` | NUMERIC(24,8) | 子任务奖励（新字段）| ⚠️ 存在但混淆 |

**混淆点**：
- `max_amount_nt` vs `reward_nt` 的语义不清
- 应该只用一个字段表示子任务总金额

---

## 🔧 修复建议

### 优先级1（必须）
- [ ] 添加 `checkSubtaskTotalRewardGate()` 函数（参考 deadline gate）
- [ ] 在 `createSubtask`、`patchSubtask`、`finalizeSubtasks` 中调用验证
- [ ] 验证逻辑：`sum(subtask.reward_nt * subtask.participant_limit) <= task_info.planned_lock_nt`

### 优先级2（重要）
- [ ] 更新前端编辑页面，改为"任务池总积分"输入（而非"人数+每人金额"）
- [ ] 将编辑时的 `reward` 和 `participantLimit` 转换为"总金额"后再提交
- [ ] 后端 `patchTaskPoolDraft` 直接接收 `plannedLockNt`

### 优先级3（增强）
- [ ] 前端管理页显示子任务金额总计与剩余额度
- [ ] 定稿前提示超额警告

---

## 📄 相关文档

核心业务文档：
- [BAI_TASKPOOL_SUBTASK_RULES_V1.md](mycoseed-frontend/docs/BAI_TASKPOOL_SUBTASK_RULES_V1.md) - 子任务规则  
- [BAI_TASKPOOL_ONCHAIN_SEMANTICS_V0.md](mycoseed-frontend/docs/BAI_TASKPOOL_ONCHAIN_SEMANTICS_V0.md) - 链上语义

相关代码：
- [checkSubtaskSubmitDeadlineGate](src/controllers/taskInfoController.ts#L155) - 参考验证模式
- [TaskPoolLogicV2.sol](contracts-project/contracts/TaskPoolLogicV2.sol#L1) - 合约层资金约束

---

## 测试场景

```gherkin
场景1：超额创建子任务（应被拦截）
  已知：任务池总积分 = 100
  当：Manager 创建子任务1（奖励60）✓
  且：Manager 创建子任务2（奖励60）✓
  当：点击定稿
  则：后端返回 400 "子任务总金额(120) > 任务池总激励(100)"
  
场景2：编辑修正超额子任务
  已知：子任务总金额 = 120（超过100的池额）
  当：Manager 编辑子任务2（改为30）
  且：点击定稿
  则：编辑成功，定稿成功，总金额 = 90 ✓
```

---

## 完成检查清单

- [x] 数据库字段完整性 ✅
- [x] 后端 API 逻辑 ⚠️ **缺约束验证**
- [x] 前端创建流程 ✅ **已改为总金额**
- [x] 前端编辑流程 ❌ **未同步改动**
- [ ] 子任务金额约束 ❌ **未实现**
- [ ] 前端展示与提示 ⚠️ **缺总计显示**
