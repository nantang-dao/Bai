# 任务池激励改进 - 修复实施方案

## 问题汇总

### 🔴 关键缺陷

1. **后端缺失子任务总金额约束**
   - 无法防止 manager 分配超过池总激励的子任务
   - 定稿前无验证，定稿后无法回滚

2. **前端编辑页面未同步改动**
   - 创建用"任务池总积分"单一值
   - 编辑仍用"人数 × 每人金额"组合
   - 导致数据不一致

3. **后端 patchTaskPoolDraft 逻辑错误**
   - 直接计算 `planned_lock_nt = reward × participantLimit`
   - 但前端新模式应该直接传"总金额"

---

## 修复方案

### 方案A - 推荐（完整统一）

#### 后端改动

**1. 新增验证函数** - [taskInfoController.ts](src/controllers/taskInfoController.ts)

```typescript
/**
 * 校验子任务总金额不超过任务池总激励
 * 规则：sum(subtask.reward_nt × subtask.participant_limit) <= task_info.planned_lock_nt
 */
function checkSubtaskTotalRewardGate(
  taskInfo: { planned_lock_nt?: string | number | null },
  subtasks: Array<{ reward_nt?: string | number | null; participant_limit?: number | null }>
): { ok: true } | { ok: false; message: string } {
  const poolTotal = taskInfo.planned_lock_nt != null ? Number(taskInfo.planned_lock_nt) : NaN
  if (!poolTotal || poolTotal <= 0) {
    return { ok: false, message: '任务池总激励无效' }
  }

  const subtaskTotal = subtasks.reduce((sum, s) => {
    const reward = s.reward_nt != null ? Number(s.reward_nt) : 0
    const participants = s.participant_limit ?? 1
    return sum + reward * participants
  }, 0)

  if (subtaskTotal > poolTotal) {
    return { ok: false, message: `子任务总金额(${subtaskTotal.toFixed(2)}) 不得超过任务池总激励(${poolTotal.toFixed(2)})` }
  }
  return { ok: true }
}
```

**2. 修改 createSubtask** - [L199+](src/controllers/taskInfoController.ts#L199)

```typescript
export const createSubtask = async (req: AuthRequest, res: Response) => {
  // ... 现有代码 ...
  
  // 【修改】：创建前先获取所有现有子任务并验证
  const { data: existingSubtasks } = await supabase
    .from('task_subtasks')
    .select('reward_nt, participant_limit')
    .eq('task_info_id', taskInfoId)
  
  // 【新增】：包含当前要创建的子任务进行验证
  const allSubtasksForValidation = [
    ...(existingSubtasks || []),
    {
      reward_nt: rewardNt ?? 0,
      participant_limit: participantLimit ?? 1
    }
  ]
  
  const gate2 = checkSubtaskTotalRewardGate(gate.taskInfo, allSubtasksForValidation)
  if (!gate2.ok) return res.status(400).json({ error: gate2.message })
  
  // ... 现有的创建逻辑 ...
}
```

**3. 修改 patchSubtask** - [L260+](src/controllers/taskInfoController.ts#L260)

```typescript
export const patchSubtask = async (req: AuthRequest, res: Response) => {
  // ... 现有验证代码 ...

  // 【新增】：获取该子任务原值 + 所有其他子任务
  const { data: thisSubtask } = await supabase
    .from('task_subtasks')
    .select('reward_nt, participant_limit')
    .eq('id', subtaskId)
    .single()
  
  const { data: otherSubtasks } = await supabase
    .from('task_subtasks')
    .select('reward_nt, participant_limit')
    .eq('task_info_id', taskInfoId)
    .neq('id', subtaskId)
  
  // 【新增】：构建修改后的验证列表（用patch中的新值覆盖原值）
  const updatedSubtask = {
    reward_nt: patch.reward_nt ?? thisSubtask?.reward_nt,
    participant_limit: patch.participant_limit ?? thisSubtask?.participant_limit
  }
  const allForValidation = [updatedSubtask, ...(otherSubtasks || [])]
  
  const gate2 = checkSubtaskTotalRewardGate(gate.taskInfo, allForValidation)
  if (!gate2.ok) return res.status(400).json({ error: gate2.message })
  
  // ... 现有的更新逻辑 ...
}
```

**4. 修改 finalizeSubtasks** - [L350+](src/controllers/taskInfoController.ts#L350)

```typescript
export const finalizeSubtasks = async (req: AuthRequest, res: Response) => {
  // ... 现有验证 ...
  
  // 【新增】：定稿前最终验证一次
  const { data: allSubtasks } = await supabase
    .from('task_subtasks')
    .select('reward_nt, participant_limit')
    .eq('task_info_id', taskInfoId)
  
  const gate2 = checkSubtaskTotalRewardGate(gate.taskInfo, allSubtasks || [])
  if (!gate2.ok) {
    return res.status(400).json({ 
      error: `无法定稿：${gate2.message}` 
    })
  }
  
  // ... 后续逻辑保持不变 ...
```

**5. 修改 patchTaskPoolDraft** - [L829](src/controllers/taskInfoController.ts#L829)

```typescript
// 【修改】：改为接收总金额，而非人数×金额
// 前端新的PATCH body 应该包含：
// { plannedLockNt: 100 } 而不是 { reward: 50, participantLimit: 2 }

// 但为了兼容，保留旧逻辑：
if (body.plannedLockNt != null) {
  // 如果前端明确传递了总金额
  plannedLockNt = Number(body.plannedLockNt)
} else if (body.reward != null && body.participantLimit != null) {
  // 兼容旧的人数×金额方式（废弃）
  console.warn('[DEPRECATED] 使用了旧的 reward×participantLimit 方式，请改为直接传 plannedLockNt')
  plannedLockNt = Number(body.reward) * Number(body.participantLimit)
} else {
  // 都没有，保留原值
  plannedLockNt = (ti as any).planned_lock_nt
}

// 【新增】：修改总金额时，验证现有子任务是否超额
if (body.plannedLockNt != null && gate.taskInfo.use_taskpool) {
  const { data: allSubtasks } = await supabase
    .from('task_subtasks')
    .select('reward_nt, participant_limit')
    .eq('task_info_id', taskInfoId)
  
  const gate2 = checkSubtaskTotalRewardGate(
    { planned_lock_nt: plannedLockNt },
    allSubtasks || []
  )
  if (!gate2.ok) {
    return res.status(400).json({
      error: `无法修改总激励：${gate2.message}`
    })
  }
}
```

#### 前端改动

**1. 统一编辑页面** - [edit.vue](pages/tasks/pool/[taskInfoId]/edit.vue)

```vue
<!-- 【修改】原来的"参与人数" + "每人积分" 改为"任务池总积分" -->
<template>
  <div class="space-y-4">
    <!-- ... 其他字段 ... -->
    
    <!-- 【移除】这两个字段 -->
    <!-- <div>参与人数</div> -->
    <!-- <div>每人积分</div> -->
    
    <!-- 【新增】用总金额替代 -->
    <div>
      <label class="block text-xs font-bold uppercase mb-1 text-text-title">任务池总积分 *</label>
      <input 
        v-model.number="form.plannedLockNt" 
        type="number" 
        min="1" 
        step="1"
        placeholder="任务池总预算（NT）"
        class="w-full h-12 px-4 bg-input-bg border border-border rounded-2xl" 
      />
      <p class="text-xs text-text-body/70 mt-1">
        子任务的奖励总和不得超过此值
      </p>
    </div>
  </div>
</template>

<script setup lang="ts">
const form = ref({
  title: '',
  description: '',
  plannedLockNt: '',  // 【改】总金额
  // ...
})

async function submit() {
  // ...
  await patchTaskPoolDraft(
    id,
    {
      title: form.value.title.trim(),
      description: form.value.description.trim(),
      plannedLockNt: parseFloat(form.value.plannedLockNt),  // 【改】直接传总金额
      startDate,
      deadline: form.value.deadline,
      submitDeadline: form.value.submitDeadline,
    },
    baseUrl
  )
}

onMounted(async () => {
  // ...
  const draft = await getTaskPoolDraftForEdit(id, baseUrl)
  // 【改】直接读取 plannedLockNt
  if (draft.plannedLockNt != null) {
    form.value.plannedLockNt = String(draft.plannedLockNt)
  }
})
</script>
```

**2. 增强管理页面** - [manage.vue](pages/tasks/pool/[taskInfoId]/manage.vue)

```vue
<!-- 【在子任务列表下方新增】总计卡片 -->
<PixelCard v-if="subtasks.length" class="mb-6 bg-amber-50 dark:bg-amber-950">
  <h3 class="font-bold text-text-title mb-3">子任务金额统计</h3>
  
  <div class="space-y-2 text-sm">
    <div class="flex justify-between">
      <span class="text-text-body">任务池总积分：</span>
      <span class="font-mono font-bold">{{ taskMeta?.plannedLockNt ?? '—' }}</span>
    </div>
    
    <div class="flex justify-between">
      <span class="text-text-body">子任务已分配：</span>
      <span class="font-mono font-bold">{{ subtotalReward.toFixed(2) }}</span>
    </div>
    
    <div 
      :class="['flex justify-between pt-2 border-t border-amber-200', {
        'text-red-600 dark:text-red-400': subtotalReward > (taskMeta?.plannedLockNt ?? 0),
        'text-green-600 dark:text-green-400': subtotalReward <= (taskMeta?.plannedLockNt ?? 0)
      }]"
    >
      <span class="font-bold">剩余可分配：</span>
      <span class="font-mono font-bold">
        {{ Math.max(0, (taskMeta?.plannedLockNt ?? 0) - subtotalReward).toFixed(2) }}
      </span>
    </div>
    
    <div v-if="subtotalReward > (taskMeta?.plannedLockNt ?? 0)" class="pt-2 text-red-600 dark:text-red-400 text-xs">
      ⚠️ 子任务总金额已超过任务池总积分，无法定稿！请删除或减少部分子任务。
    </div>
  </div>
</PixelCard>

<script setup>
const subtotalReward = computed(() => {
  return (subtasks.value || []).reduce((sum, s) => {
    const reward = s.rewardNt ?? 0
    const participants = s.participantLimit ?? 1
    return sum + (reward * participants)
  }, 0)
})

// 【改】定稿前检查
async function onFinalize() {
  if (subtotalReward.value > (taskMeta.value?.plannedLockNt ?? 0)) {
    toast.add({
      title: '无法定稿',
      description: '子任务总金额超过了任务池总积分',
      color: 'red'
    })
    return
  }
  // ... 继续定稿 ...
}
</script>
```

---

## 方案B - 过渡方案（如果不想改前端太多）

如果当前改不了前端编辑页面，可以保留现状但在后端做防护：

1. 后端保留 `reward × participantLimit` 的计算
2. 但**务必**添加子任务总金额的约束验证（方案A中的 checkSubtaskTotalRewardGate）
3. 标记编辑页为过时，计划下个版本统一

---

## 实施步骤

### 第1步：后端改动（1-2小时）
```bash
cd bai/mycoseed-backend

# 在 taskInfoController.ts 中：
# 1. 添加 checkSubtaskTotalRewardGate 函数
# 2. 修改 createSubtask（添加验证）
# 3. 修改 patchSubtask（添加验证）
# 4. 修改 finalizeSubtasks（添加验证）
# 5. 修改 patchTaskPoolDraft（改为接收 plannedLockNt 或兼容旧方式）

# 测试
npm run test:taskpool-subtask-draft-fields
npm run test:taskpool-subtask-overrides
```

### 第2步：前端改动（1小时）
```bash
cd bai/mycoseed-frontend

# 修改：
# 1. pages/tasks/pool/[taskInfoId]/edit.vue（改为总金额）
# 2. pages/tasks/pool/[taskInfoId]/manage.vue（显示总计+警告）

npm run test:e2e:taskpool
```

### 第3步：测试&验证（1小时）
- 测试创建任务池（用总金额）
- 测试编辑任务池（改总金额）
- 测试添加超额子任务（应被后端拒绝）
- 测试正常流程（总金额正常，定稿成功）

---

## 回归测试用例

```typescript
describe('子任务金额约束', () => {
  it('应拒绝超额子任务创建', async () => {
    // 1. 创建任务池（总积分=100）
    const pool = await createPool({ plannedLockNt: 100 })
    
    // 2. Manager 创建子任务1（奖励60×1人）
    await createSubtask(pool.id, { rewardNt: 60, participantLimit: 1 })
    
    // 3. 尝试创建子任务2（奖励60×1人，总计120 > 100）
    const res = await createSubtask(pool.id, { 
      rewardNt: 60, 
      participantLimit: 1 
    })
    expect(res.status).toBe(400)
    expect(res.body.error).toContain('不得超过')
  })

  it('定稿前应验证总金额', async () => {
    const pool = await createPool({ plannedLockNt: 100 })
    await createSubtask(pool.id, { rewardNt: 101, participantLimit: 1 })
    
    const res = await finalize(pool.id)
    expect(res.status).toBe(400)
  })

  it('修改总额时应验证现有子任务', async () => {
    const pool = await createPool({ plannedLockNt: 100 })
    await createSubtask(pool.id, { rewardNt: 90, participantLimit: 1 })
    
    // 尝试降低总额到80
    const res = await patchTaskPoolDraft(pool.id, { plannedLockNt: 80 })
    expect(res.status).toBe(400)
    expect(res.body.error).toContain('已分配90')
  })
})
```

---

## 风险评估

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| 编辑页前端改动导致UI崩溃 | 低 | 中 | 灰度发布、测试充分 |
| 旧数据与新验证冲突 | 中 | 中 | 提供迁移脚本检查现有超额池 |
| 链上与链下金额不一致 | 低 | 高 | 定稿前验证，建池时再次确认 |

---

## 参考文档

- [TaskPool子任务规则](../../mycoseed-frontend/docs/BAI_TASKPOOL_SUBTASK_RULES_V1.md)
- [现有deadline验证参考](src/controllers/taskInfoController.ts#L155)
