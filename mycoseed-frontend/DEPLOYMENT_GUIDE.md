# 部署和版本管理指南

## 📋 当前状态

### Git 状态
- **当前分支**: `feature/update-20251215`
- **最新提交**: `998099b` (2025-12-23) - "timetable"
- **未提交的更改**: 8个文件已修改，2个新文件未跟踪

### 未提交的文件
- `components/layout/BottomNav.vue` (已修改)
- `components/layout/Header.vue` (已修改)
- `layouts/default.vue` (已修改)
- `pages/community/[id].vue` (已修改)
- `pages/index.vue` (已修改)
- `pages/tasks/[id].vue` (已修改)
- `pages/tasks/index.vue` (已修改)
- `pages/wallet.vue` (已修改)
- `pages/activities/create.vue` (新文件)
- `stores/community.ts` (新文件)

## 🔍 如何查看在线历史版本

### 1. 在 GitHub 上查看提交历史
访问仓库：https://github.com/Crocs77/mycoseed-frontend

**查看提交历史**:
- 点击仓库主页的 "commits" 链接
- 或访问：https://github.com/Crocs77/mycoseed-frontend/commits/feature/update-20251215

**查看特定提交的代码**:
- 点击提交哈希（如 `998099b`）
- 可以查看该提交修改了哪些文件
- 点击文件名可以查看具体的代码变更

**查看特定文件的历史版本**:
- 进入文件页面
- 点击 "History" 按钮
- 可以查看该文件的所有历史版本

### 2. 在本地查看历史版本

```bash
# 查看提交历史
git log --oneline --graph --all

# 查看特定提交的详细信息
git show <commit-hash>

# 查看特定文件的历史
git log --follow -- <file-path>

# 查看特定提交时的文件内容
git show <commit-hash>:<file-path>

# 比较两个提交之间的差异
git diff <commit1> <commit2>
```

### 3. 查看远程分支

```bash
# 查看所有远程分支
git branch -r

# 查看远程分支的最新提交
git log origin/feature/update-20251215 --oneline -10

# 查看远程和本地的差异
git fetch origin
git log HEAD..origin/feature/update-20251215
```

## 🚀 提交和部署最新代码

### 步骤 1: 查看更改内容

```bash
# 查看所有更改的统计
git diff --stat

# 查看具体更改内容
git diff

# 查看新文件
git status
```

### 步骤 2: 提交更改

```bash
# 添加所有更改
git add .

# 或者选择性添加
git add <file-path>

# 提交更改
git commit -m "feat: 添加昨晚的功能更新

- 更新Header组件，添加社区切换功能
- 更新首页，优化社区展示
- 更新任务页面，添加新功能
- 添加活动创建页面
- 添加社区store管理"

# 推送到远程
git push origin feature/update-20251215
```

### 步骤 3: 部署到生产环境

根据你的部署平台（Vercel、Netlify、或其他），部署方式可能不同：

**如果使用 Vercel**:
- 连接到 GitHub 仓库后，Vercel 会自动检测推送并部署
- 或手动触发：在 Vercel 控制台点击 "Redeploy"

**如果使用 Netlify**:
- 同样会自动部署，或手动触发部署

**如果使用其他平台**:
- 查看平台文档了解部署流程

## 📝 检查清单

在部署前，请确认：

- [ ] 所有更改已测试通过
- [ ] 代码已提交到 Git
- [ ] 代码已推送到远程仓库
- [ ] 部署平台已连接到正确的分支
- [ ] 环境变量已正确配置
- [ ] 构建命令正确（`yarn build` 或 `npm run build`）

## 🔄 回滚到之前的版本

如果需要回滚到之前的版本：

```bash
# 查看提交历史
git log --oneline

# 回滚到特定提交（创建新提交）
git revert <commit-hash>

# 或重置到特定提交（危险操作，会丢失后续提交）
git reset --hard <commit-hash>
```

## 📞 需要帮助？

如果遇到问题：
1. 检查 Git 状态：`git status`
2. 查看未提交的更改：`git diff`
3. 查看远程状态：`git fetch && git status`
4. 检查部署平台的构建日志


